# Tech Debt Tracker

Known debt and deviations from [`../golden-principles.md`](../golden-principles.md).
Each entry needs an exit criterion so an agent can pick it up and know when it
is done. Remove entries in the same PR that resolves them.

| ID | Area | Debt | Exit criterion |
|----|------|------|----------------|
| TD-10 | hardware shutdown | POSIX serial `tcdrain` has no timeout; third-party camera/CoreMOR discovery calls cannot be interrupted once entered. Since #419 every scan is a `DeviceDiscoveryService` job: cancellation is honoured between provider steps (endpoints, Modbus addresses) and `shutdownDiscovery()` joins the worker after the current vendor call returns (`IDeviceDiscoveryProvider::cancellable()` documents the bound per provider; camera SDK providers are `false`). An arbitrary hung vendor enumeration can still delay shutdown. | Add a stalled-output POSIX regression and bounded drain; define vendor cancellation or process isolation for the camera SDK providers, and prove close during a stalled probe releases ownership without unsafe thread detachment. |
| TD-11 | device discovery | #419 hardware acceptance is partial (rig PC 2026-09-16): startup discovery, auto-selection (MindVision), auto-connection (OEABT nanopositioner on COM7), shutdown ordering, close-during-scan, and relaunch verified on real hardware. GUI-interactive tests (Refresh, Cancel, Scan buttons, active-device conflict during capture, unplug/replug) and pulse-generator Scan were not tested (non-interactive session). eGrabber and CoreMorrow not installed. Full matrix in the execution plan. | Complete remaining GUI-interactive tests in a manual session; test pulse-generator Scan/Cancel on COM6; then move the plan to `completed/`. |
| TD-12 | device discovery | `NanopositionerTab` no longer enumerates ports on construction, so its endpoint combo is empty until the first discovery job (startup or Refresh) delivers candidates; the backend-vendor combo filters the cached list only. | Either accept (document in the user manual) or have the adapter deliver the last completed nanopositioner snapshot to a newly shown tab. |
| TD-13 | bridge | The Tauri shell never starts `StartupDiscoveryCoordinator` (no auto-selection, as before #419) and, if it did, the inline executor would run `AppBackend` selection setters on a discovery worker while Tauri commands read them. | Decide whether the shell wants startup auto-selection; if so give the coordinator a bridge-mutex executor or make the selection setters thread-safe. |
| TD-15 | tooling | `scripts/doctor.ps1` / `scripts/bootstrap.ps1` have been executed only under PowerShell 7 on Linux (emulated; 2026-09-21: found and fixed a CommandNotFound leak and a `-pr` → `-ProgressAction` binding bug) and parse-checked by `ci.yml`; the winget, vswhere, `cl.exe /showIncludes` and `conan install` paths have not run on a Windows host. `release.ps1`'s move to `scripts/release/` is proven by the `develop` auto-beta build (v1.1.2-beta.72dcfd1). | Run doctor → bootstrap (`-Generator Ninja`) → `cmake --preset windows-ninja` on the rig or bench PC once; record in a task note; remove this entry. |
| TD-17 | frontend | The Review tab converts stored pixel areas to µm² with the *current* backend pixel-to-micron factor, not the one the file was recorded with (pre-existing for its scatter). The full-run KDE core contour follows the scatter so they overlay, and records the factor it used, but a file recorded under another calibration is shown and contoured on today's scale. | Review reads the recorded factor (run snapshot `pixel_to_micron` / `config_json`) for both the scatter and the full-run contour, falling back to the current factor only for legacy files, with a test on a file recorded at a different factor |
| TD-16 | frontend | The Monitoring tab's plain deformability-vs-area scatter (one `QScatterSeries` holding up to 1000 points, cleared and re-appended every 500 ms) stalls the GUI thread ~380 ms per refresh on a desktop PC with the mock camera at 200 fps (`integration.monitoring_kde_e2e`, 2026-09-23: 5 ms-tick p99/max 376/402 ms with KDE off). The same 1000 points spread over the eight density-level series repaint in ~45 ms, so the single large series (Qt Charts per-series geometry rebuild) is the suspect, not the thumbnails. Pre-existing; the KDE work only measured it. | Profile `updateScatterplot` with KDE off; either chunk the plain series the way the density levels do or append incrementally instead of clear+append, and gate it with the e2e's GUI-tick metric (p99 under ~60 ms at 1000 points). |
| TD-1 | tooling | `.clang-format` exists but the existing codebase has never been bulk-formatted; CI does not gate on format | Decide: bulk-format in one commit + add CI gate, or keep format-on-touch policy |
| TD-2 | docs | `docs/superpowers/plans/` predates `docs/exec-plans/`; the two 2026-04-16 plans were never marked completed | Verify both plans shipped, annotate status, and stop adding plans to `superpowers/` |
| TD-3 | docs | Vendor material (`docs/Longer Pump dLSP501 Modbus RTU Series/`, `docs/mib_grabber.cpp`) sits loose in `docs/` | Move under `docs/integration/` or a `docs/vendor/` folder and link from the index |
| TD-4 | testing | No CI coverage for Windows build of the full app (only packaging-script validation in `ci.yml`; full builds are manual `workflow_dispatch`) | Scheduled or PR-triggered Windows build job, or documented decision not to |
| TD-5 | tooling | No pre-commit hook; doc/vault checks rely on agents remembering `scripts/check_docs.py` | Add pre-commit config running `check_docs.py` (and clang-format on staged C++) |
| TD-7 | realtime | Trigger and realtime-inline threads run at default OS priority; trigger-onset jitter under load is tens-to-hundreds of µs (documented `TriggerService` limitation; the `e2e_trigger_timing_test` ≤50 ms gate is currently met). Related shutdown-only latency: `EGrabberCamera::stop()` sleeps ~360 ms under `stateMutex_`. See `docs/exec-plans/active/2026-07-02-realtime-performance.md` (P9) | Opt-in (default off) priority raise for the trigger + realtime threads — Windows `SetThreadPriority`, best-effort `pthread_setschedparam` — validated by `e2e_trigger_timing_test` on hardware, with documented rollback; or a documented decision not to |
| TD-8 | frontend | `ConfigTabs` enumerates serial ports synchronously on the GUI thread during construction (`restorePulseGenSettings` → `refreshPulseGenPorts` → `QSerialPortInfo::availablePorts`); `pulse_generator` in `MIB_DISABLED_SERVICES` does not suppress it. The one-time hardware-host frontend stall did not reproduce, so this is not claimed as its root cause. See [MindVision acceptance evidence](../evidence/2026-09-07-mindvision-acceptance/#frontend-lane-on-mindvision-bench). | Defer enumeration until the MindVision page is first shown, run it off-thread, honour the `pulse_generator` disable token, and add a bounded regression test proving disabled construction/show never enumerates ports. |
| TD-9 | testing | No pull-request lane compiles or runs the Qt frontend tests (`tests/frontend/*`, e.g. `frontend.config_tabs_state`); `backend-ci.yml` and `sanitizers.yml` use `linux-backend-only`, so `src/frontend` changes and their real-widget regressions are verified only by whoever has a local Qt build. Surfaced on 2026-09-14 when the illuminated Live View review pass had to land frontend fixes unverified from a host with no toolchain. | A Linux Qt lane (`linux-system-release` preset, offscreen platform) that builds and runs `frontend.*` tests on PRs touching `src/frontend`, `include/frontend` or `tests/frontend`; or a documented decision not to |
