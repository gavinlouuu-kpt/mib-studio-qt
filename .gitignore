MIB-Studio/
build/
CMakeUserPresets.json
export/
data/logs/app.log
data/logs/symphony-*.log
data/empty_detection_results/
data/conformance/
review_artifacts/
.cache/
.python_deps/

# Exclude .exe files in installers directory
resources/installers/*.exe
resources/installers/build/**/*.exe
credentials.json

# Test files
test_installer.exe
test_*.exe
test_*.txt
test_*.json

**/.venv/

# Tools build output (binaries published, not committed)
tools/dist/
tools/build/

# Kedro frame detection pipeline
scripts/kedro_frame_detection/data/
scripts/kedro_frame_detection/conf/local/
**/__pycache__/
**/*.egg-info/
**/.pytest_cache/

# Python wheel build output (published to GitHub Releases, not committed)
bindings/python/dist/

# MCU firmware subproject (not part of Qt app release)
mcu/

# MkDocs user-guide site build output (docs-site.yml / mkdocs build)
site/
/build-*/

# Per-machine agent settings (may contain permission allowlists with local paths or credentials)
.claude/settings.local.json
.claude/*.local.*

# Build/debug logs at the repo root and agent-runner state never belong in git
# (evidence bundles under docs/evidence/ keep their logs deliberately)
/*.log
data/logs/
